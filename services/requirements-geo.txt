# Geo pipeline deps (Step 7). Heavier; kept separate so the API image stays light.
# Used by the `geo` worker image (services/Dockerfile.geo), NOT the API image.
earthaccess==0.12.0
pystac-client==0.8.5
rioxarray==0.18.2
rasterio==1.4.3
xarray==2025.1.1
numpy==2.2.1
shapely==2.0.6
geopandas==1.0.1
pyproj==3.7.0
mgrs==1.5.0
psycopg[binary]==3.2.3
# C3 tap-to-detect boundary microservice (segment_api.py) — served from this image.
fastapi==0.115.6
uvicorn==0.34.0
